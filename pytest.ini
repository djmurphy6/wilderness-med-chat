[pytest]
testpaths = tests
markers =
    unit: Fast, deterministic tests. No Ollama or ChromaDB required.
    integration: Requires Ollama running and models downloaded.
    scenario: Behavioral eval tests. Requires Ollama running.
addopts = -v --tb=short
