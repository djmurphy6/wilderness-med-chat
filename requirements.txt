# LLM
ollama>=0.3.0

# RAG
llama-index-core>=0.11.0
llama-index-vector-stores-chroma>=0.2.0
llama-index-embeddings-ollama>=0.3.0
llama-index-readers-file>=0.2.0
chromadb>=0.5.0

# STT — works on Mac (CPU) and Jetson Nano (CUDA), unlike mlx-whisper
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.26.0

# TTS — Kokoro-82M via ONNX (cross-platform: Mac CPU + Jetson CUDA)
kokoro-onnx>=0.3.0
soundfile>=0.12.1

# Utilities
python-dotenv>=1.0.0
rich>=13.7.0

# Eval (cloud judge — test pipeline only, not used in production)
ragas>=0.2.0
langchain-google-genai>=1.0.0
langchain-community>=0.3.0
google-genai>=1.0.0
datasets>=2.18.0
