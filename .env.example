# Copy this to .env and fill in your values.
# .env is gitignored — never commit real keys.

# Required for RAGAS eval (cloud judge only — NOT used in production)
GOOGLE_API_KEY=AIza...

# Optional: override default Ollama host (default: http://localhost:11434)
# OLLAMA_HOST=http://localhost:11434
